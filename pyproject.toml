[project]
name = "brain-games"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Artem Mitasov", email = "mitasov.artem@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
brain-games = "brain_games:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
